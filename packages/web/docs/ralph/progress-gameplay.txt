Progress log for PRD gameplay implementation.

## 2026-03-08 — Phase 3.1: Categorized lobby sections

Implemented four categorized sections in the lobby page (LobbyPage.tsx):

1. **Your Tables** — Amber/orange styled cards for games the current user is in
   (status=playing + user in player_ids/seats). Shows "Rejoin" button that navigates
   directly to the game page (no API call needed).

2. **Open Tables** — Main section showing waiting rooms with "Join" button.
   Uses existing RoomTable component. Shows empty state when no rooms available.

3. **Need a Player** — Dashed-border cards for playing rooms with vacant seats.
   Shows "Substitute" button (calls joinRoom API).

4. **Watch** — Horizontal scrolling compact cards for spectatable games (playing,
   full seats, not the user's game). Shows "Watch" button.

Categorization is done client-side via `categorizeRooms()` in LobbyPage.tsx. The
server still sends a flat rooms array; if it ever sends categorized data, the
channel hook can be updated to handle it.

Also fixed:
- Updated RoomRow to use generic `onAction`/`actionLabel` interface instead of
  hardcoded Join-only props. Fixed error display bug (errors now persist after
  loading completes instead of never showing).
- Updated RoomTable to match new interface (actionLoadingCode, actionError).
- Fixed pre-existing lint errors in TrickArea.tsx (array index keys).
- Fixed all formatter issues across the codebase.
- Added 4 new tests for categorized sections (13 total lobby tests).
- Also marked PRD feature 3.2 (create table + online count) as passing since it
  was already fully implemented.

All checks pass: typecheck, lint, 111 tests.

## 2026-03-08 — Phase 6.2: Seat status indicators for disconnected/bot players

Implemented seat lifecycle event handling and visual indicators for disconnect
scenarios in the game table.

**Channel events added** (useGameChannel.ts):
- `player_reconnecting` — Sets seat to 'reconnecting' status, shows warning toast
- `bot_substitute_active` — Sets seat to 'bot_substitute', shows warning toast
- `player_reclaimed_seat` — Resets seat to 'normal', shows success toast
- `seat_permanently_botted` — Sets seat to 'permanent_bot', updates username to 'Bot'
- `substitute_joined` — Resets seat to 'normal' with new username, shows success toast

**Store changes** (shared/stores/game.ts):
- Added `seatStatus: SeatStatus` field to `PlayerMeta` interface
- Added `setSeatStatus(position, status, username?)` action
- `SeatStatus` flows through `RelativePlayerView` to the view model

**UI changes** (GamePlayerCard.tsx):
- Reconnecting state: amber spinner + "Reconnecting..." text, dimmed opacity
- Bot substitute state: robot emoji avatar, "Bot" badge (amber), subtext
  "Bot · [PlayerName] can rejoin"
- Permanent bot state: robot emoji avatar, "Bot" badge, simple "Bot" subtext
- DC badge only shown for plain disconnects (not reconnecting or bot states)

**Other changes:**
- Added `amber` variant to Badge component
- PlayerHand passes seatStatus through to GamePlayerCard
- GameTable passes seatStatus from viewModel to each PlayerHand
- GamePage passes onSeatEvent callback to useGameChannel for toast notifications
- Fixed pre-existing TrickArea.tsx typecheck error (trick.number → idx)
- Fixed pre-existing TrickArea.tsx lint warning (array index key → composite key)

**PRD updates:**
- Marked all previously-implemented features as passing (1.1-1.5, 2.1-2.2,
  4.1-4.4, 5.1-5.3, 6.1) — these were already built but the PRD wasn't updated
- Marked 6.2 as passing

**Remaining features:** 6.3 (owner decision banner), 7.1-7.3 (polish)

All checks pass: typecheck, lint, 111 tests.

## 2026-03-08 — Phase 6.3: Owner decision banner for substitute seats

Implemented the owner decision banner that appears when a player disconnects and
the room owner needs to decide whether to open the seat for substitutes or keep
the bot.

**Channel event added** (useGameChannel.ts):
- `owner_decision_available` — Fires when a player disconnects and the server
  offers the room owner a choice. Calls new `onOwnerDecision` callback with
  `{ position, playerName }`.
- Added `OwnerDecisionEvent` type and `onOwnerDecision` option to hook interface.

**GamePage changes** (GamePage.tsx):
- Stores `hostId` in `roomConfigRef` from the Room API response.
- Maintains `ownerDecisionQueue` state (array of pending decisions) and
  `dismissedSeatsRef` (Set of positions already dismissed).
- `handleOwnerDecision`: only enqueues if current user is the room owner AND
  position hasn't been dismissed AND isn't already in the queue.
- `handleOpenSeat`: dismisses the position, removes from queue, pushes
  `open_seat` action to the game channel.
- `handleKeepBot`: dismisses the position and removes from queue (no server call).
- Banner is hidden during the owner's active turn (`isMyTurn` check) — queued
  events surface when the turn passes.
- Only one banner shown at a time (always `queue[0]`).

**New component** (OwnerDecisionBanner.tsx):
- Non-blocking amber banner positioned at the top of the game area (z-40).
- Shows "[PlayerName] left. Open their seat for another player?"
- Two buttons: "Open Seat" (amber solid) and "Keep Bot" (amber outline).
- Uses existing `animate-slide-in-top` CSS animation.

**Remaining features:** 7.1-7.3 (polish)

All checks pass: typecheck, lint, 111 tests.

## 2026-03-08 — Phase 7.2: Loading states and error handling throughout the app

Improved loading states, error handling, and added optimistic UI across the app.

**GamePage error improvements** (GamePage.tsx):
- Differentiated room errors by HTTP status: 404 → "Room not found. It may have
  been closed." / 403 → "You do not have access to this room." / other → "Failed
  to connect to server."
- Added "Retry" button to both room error and channel error states (alongside
  existing "Back to Lobby" button). Retry re-fetches the room and reconnects
  the channel. Room-not-found errors only show "Back to Lobby" (no retry).
- Extracted `fetchRoom()` as a reusable callback for both initial load and retry.
  Uses a `fetchIdRef` counter to cancel stale fetches.

**Optimistic card play** (GamePage.tsx, GameTable.tsx, TrickArea.tsx):
- When a player clicks a card to play it, the card immediately moves from their
  hand to the trick area (south slot) before the server responds.
- `optimisticCard` state in GamePage flows to GameTable → TrickArea.
- GameTable filters the optimistic card from the south player's hand so it
  disappears instantly.
- TrickArea adds the optimistic card to the south trick slot if not already
  present from server state.
- Cleared automatically when server state updates (confirming the play).
- On error: cleared immediately, card reappears in hand with shake animation
  and error toast.

**Lobby channel error** (useLobbyChannel.ts, LobbyPage.tsx):
- Lobby channel join failures now call `setError()` on the lobby store (previously
  only logged to console).
- LobbyPage reads the error state and shows a red error banner with "Retry" button
  when the lobby channel fails to connect.

**Test updates:**
- Updated GamePage test to match new error message text and verify Retry button.

**Remaining features:** 7.1 (responsive layout), 7.3 (turn indicators/game feel)

All checks pass: typecheck, lint, 111 tests.

## 2026-03-08 — Phase 7.3: Turn indicators, timing, and game feel

Added visual polish for turn indicators, card animations, trick win highlights,
prominent trump/bid display, and score transitions.

**Turn indicators** (GamePlayerCard.tsx, index.css):
- Added `pidro-seat-card--your-turn` CSS class with `pulse-glow` keyframe animation.
  When it's YOUR turn, the seat card border pulses between normal cyan glow and a
  brighter, wider glow (1.8s cycle). Applied only when `isYou && isCurrentTurn`.
- Existing turn indicators (cyan glow + yellow pulse dot) remain for all players.

**Card play animations** (TrickArea.tsx, index.css):
- Added directional `card-enter-{north,south,east,west}` keyframe animations.
  Cards slide in from the player's direction (24px translate + scale from 0.85).
- TrickArea tracks which trick slots are new each render via `prevSlotKeysRef`
  and applies the `animate-card-enter-{position}` class to newly appeared cards.

**Trick win animation** (TrickArea.tsx, index.css):
- Added `trick-win-flash` keyframe: golden box-shadow pulse (0->6px->0 over 1s).
- TrickArea detects when `tricks.length` increases (new trick completed), resolves
  the winner's relative position, and applies `animate-trick-win` to that slot
  for 1 second before clearing.

**Prominent trump display** (GameInfoBar.tsx):
- When trump is declared, a prominent colored banner row shows the suit symbol
  (2xl size) and suit name in the score plaque, replacing the old small key-value row.
- Trump row uses `SUIT_COLORS_RAW` for the suit's actual color (red/black).
- The small "Trump: Undeclared" row only shows when trump is not yet declared.

**Bid display during playing phase** (GameInfoBar.tsx, GameTable.tsx, game.ts):
- GameInfoBar now accepts `currentBid` and `bidWinner` props.
- During playing phase (and declaring/discarding/second_deal), shows
  "Bid N (Us/Them)" alongside the trump banner.
- Store normalization updated to extract `bid_winner` from `highest_bid.position`
  when not explicitly provided by the server.

**Score transitions** (GameInfoBar.tsx, index.css):
- Added `score-bump` keyframe (scale 1->1.15->1 over 0.4s).
- GameInfoBar tracks previous scores via ref and applies `animate-score-bump`
  class to the "Us" or "Them" score number when it changes.

**Other changes:**
- Updated DesignSystemPage to include new `currentBid`/`bidWinner` props.

**Remaining features:** 7.1 (responsive layout)

All checks pass: typecheck, lint, 111 tests.

## 2026-03-08 — Phase 7.1: Responsive layout — game table works on various screen sizes

Added tablet breakpoint (`max-lg` at 1024px) and improved responsive behavior across
the app. The codebase already had good mobile (`max-md`, `max-sm`) breakpoints; this
fills in the gap between desktop and phone.

**GamePage changes** (GamePage.tsx):
- Game window now fills viewport on tablets and phones: `max-lg:h-[calc(100dvh-24px)]`
  instead of fixed `min-h-[760px]`. Desktop keeps the fixed min-height.
- Inner game area uses `h-[calc(100%-20px)]` to fill below the titlebar.

**GameTable changes** (GameTable.tsx):
- Canvas adds intermediate `max-lg:aspect-[3/4]` between desktop `4/3` and
  phone `10/16` — smoother transition for tablet portrait/landscape.
- All position zones (north/south/east/west, center panel, leave button) gain
  `max-lg` intermediate values for positioning and sizing.
- South hand widens to `max-lg:w-[82%]` (between 72% desktop and 94% mobile).
- GameInfoBar centers on tablet same as mobile (`max-lg:inset-x-0 max-lg:justify-center`).

**Card changes** (Card.tsx):
- `lg` size adds `max-lg` intermediate step: 96×66px (between 104×72 desktop and
  92×62 mobile). Rank and suit font sizes also scale at `max-lg`.

**PlayerHand changes** (PlayerHand.tsx):
- Dynamic card overlap: when card count exceeds 8 (south) or 7 (east/west/north),
  overlap tightens progressively. Prevents overflow during second_deal phase when
  players hold up to 13 cards.
- Seat card sizing gains `max-lg` intermediate step for `min-w` and `max-w`.

**GamePlayerCard changes** (GamePlayerCard.tsx):
- Avatar scales at `max-lg` to 40×40px (between 48px desktop and 36px mobile).
- Role label hides at `max-lg` (was `max-md`) to save space on tablets.

**GameInfoBar changes** (GameInfoBar.tsx):
- Width shrinks to 280px at `max-lg` (between 320px desktop and 84vw mobile).
- Score text scales to `text-3xl` at `max-lg`.
- Info detail text scales at `max-lg`.

**TrickArea changes** (TrickArea.tsx):
- Center circle and empty trick slots scale at `max-lg` (80×80px, 80×56px) and
  further at `max-sm` (64×64px, 64×48px).

**CSS changes** (index.css):
- `.pidro-seat-card`: responsive min-width/padding/gap at 1024px (112px) and 640px
  (96px) breakpoints.
- `.pidro-icon-button`: responsive control size at 1024px (52px) and 640px (44px).

**LobbyPage changes** (LobbyPage.tsx):
- "Your Tables" and "Need a Player" sections use `lg:grid-cols-2` for 2-column
  layout on wider screens (single column on tablet/phone).
- Window fills viewport on tablets: `max-lg:h-[calc(100dvh-24px)]`.

**Remaining features:** None — PRD is complete.

All checks pass: typecheck, lint, 111 tests.
