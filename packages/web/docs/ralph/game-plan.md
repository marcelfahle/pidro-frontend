0a. Study @docs/ralph/game-spec.md to learn the application specifications and requirements.
0b. Study @docs/ralph/game-impl.md (if present) to understand the plan so far.
0c. Study @docs/ralph/AGENTS.md for build/test commands and codebase patterns.
0d. For reference, the application source code is in `src/`.

1. Study @docs/ralph/game-impl.md (if present; it may be incorrect) and compare existing source code in `src/` against @docs/ralph/game-spec.md. Create/update @docs/ralph/game-impl.md as a prioritized bullet list of items yet to be implemented. Don't assume functionality is missing — confirm with code search first. Consider searching for TODO, minimal implementations, placeholders, skipped tests, and inconsistent patterns.

IMPORTANT: Plan only. Do NOT implement anything.

99999. Keep @docs/ralph/game-impl.md current — future iterations depend on this to avoid duplicating efforts.
999999. If you find inconsistencies in @docs/ralph/game-spec.md, note them in @docs/ralph/game-impl.md rather than modifying the spec.
9999999. When @docs/ralph/game-impl.md becomes large, clean out completed items.
